<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.eni.module2tp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Module2TP">
        <activity android:name="fr.eni.courses.SaisieArticleActivity"></activity>
        <activity android:name="fr.eni.courses.ConfigurationActivity" />
        <activity android:name="fr.eni.courses.ListeArticlesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="fr.eni.courses.InfoUrlActivity" />
        <activity android:name="fr.eni.courses.DetailArticleActivity" />
    </application>

</manifest>